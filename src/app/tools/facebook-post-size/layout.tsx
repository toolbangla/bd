import { createToolMetadata } from "@/lib/seo";
export const metadata = createToolMetadata({ title: "Facebook Post Size", description: "Facebook post image-এর recommended size ও dimension দেখুন।", path: "/tools/facebook-post-size", keywords: ["Facebook post size", "Facebook post মাপ", "Facebook image dimension"] });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

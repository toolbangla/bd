import { createToolMetadata } from "@/lib/seo";
export const metadata = createToolMetadata({ title: "Instagram Post Size", description: "Instagram post-এর সঠিক pixel size ও dimension দ্রুত জানুন।", path: "/tools/instagram-post-size", keywords: ["Instagram post size", "Instagram post মাপ", "Instagram image dimension"] });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

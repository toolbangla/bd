import { createToolMetadata } from "@/lib/seo";
export const metadata = createToolMetadata({ title: "YouTube Thumbnail Size", description: "YouTube thumbnail-এর recommended size ও dimension দ্রুত জানুন।", path: "/tools/youtube-thumbnail-size", keywords: ["YouTube thumbnail size", "YouTube thumbnail মাপ", "video thumbnail dimension"] });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

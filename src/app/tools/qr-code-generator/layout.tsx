import { createToolMetadata } from "@/lib/seo";
export const metadata = createToolMetadata({ title: "QR Code Generator", description: "Text, URL ও contact vCard থেকে QR Code তৈরি করুন এবং PNG download করুন।", path: "/tools/qr-code-generator", keywords: ["QR code generator", "QR code তৈরি", "contact QR code", "URL QR"] });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

import { createToolMetadata } from "@/lib/seo";
export const metadata = createToolMetadata({ title: "YouTube Banner Size", description: "YouTube channel banner-এর recommended pixel dimension দেখুন।", path: "/tools/youtube-banner-size", keywords: ["YouTube banner size", "YouTube channel art মাপ", "YouTube banner dimension"] });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

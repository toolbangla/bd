import { createToolMetadata } from "@/lib/seo";
export const metadata = createToolMetadata({ title: "Image Resizer", description: "ছবির width ও height পরিবর্তন করে অনলাইনে resize করুন।", path: "/tools/image-resizer", keywords: ["image resizer", "ছবি resize", "ছবির মাপ পরিবর্তন"] });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

import { createToolMetadata } from "@/lib/seo";
export const metadata = createToolMetadata({ title: "PDF Merger", description: "একাধিক PDF একত্র করুন এবং একটি combined PDF download করুন।", path: "/tools/pdf-merger", keywords: ["PDF একত্র করুন", "PDF merger", "merge PDF online"] });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

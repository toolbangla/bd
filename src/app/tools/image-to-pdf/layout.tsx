import { createToolMetadata } from "@/lib/seo";
export const metadata = createToolMetadata({ title: "Image to PDF", description: "এক বা একাধিক ছবি একসাথে PDF ফাইলে রূপান্তর করুন।", path: "/tools/image-to-pdf", keywords: ["image to PDF", "ছবি থেকে PDF", "JPG PDF বানান"] });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

import { createToolMetadata } from "@/lib/seo";
export const metadata = createToolMetadata({ title: "বাংলা to English Text Helper", description: "বাংলা ও English লেখা প্রস্তুত, সম্পাদনা এবং গুছিয়ে নিতে সহজ text helper ব্যবহার করুন।", path: "/tools/bangla-to-english-helper", keywords: ["বাংলা English text helper", "বাংলা থেকে English helper", "Bangla text tool"] });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

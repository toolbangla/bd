import { createToolMetadata } from "@/lib/seo";
export const metadata = createToolMetadata({ title: "PDF Splitter", description: "PDF-এর প্রথম পেজ আলাদা করে নতুন PDF হিসেবে download করুন।", path: "/tools/pdf-splitter", keywords: ["PDF splitter", "PDF page আলাদা করুন", "split PDF"] });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

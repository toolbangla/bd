import { createToolMetadata } from "@/lib/seo";
export const metadata = createToolMetadata({ title: "Unit Converter", description: "Length, weight, temperature, area, volume, time, speed ও data storage unit রূপান্তর করুন।", path: "/tools/unit-converter", keywords: ["unit converter", "একক রূপান্তর", "length weight converter"] });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

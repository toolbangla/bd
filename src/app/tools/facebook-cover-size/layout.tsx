import { createToolMetadata } from "@/lib/seo";
export const metadata = createToolMetadata({ title: "Facebook Cover Size", description: "Facebook cover photo-এর recommended pixel size দ্রুত দেখুন।", path: "/tools/facebook-cover-size", keywords: ["Facebook cover size", "Facebook cover মাপ", "social media image size"] });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

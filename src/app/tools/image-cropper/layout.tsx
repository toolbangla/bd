import { createToolMetadata } from "@/lib/seo";
export const metadata = createToolMetadata({ title: "Image Cropper", description: "অনলাইনে ছবি crop করে প্রয়োজনীয় অংশ আলাদা করে download করুন।", path: "/tools/image-cropper", keywords: ["image cropper", "ছবি crop", "অনলাইনে ছবি কাটুন"] });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

import { createToolMetadata } from "@/lib/seo";
export const metadata = createToolMetadata({ title: "JPG to PDF", description: "JPG বা PNG ছবি অনলাইনে PDF ফাইলে রূপান্তর করুন।", path: "/tools/jpg-to-pdf", keywords: ["JPG to PDF", "ছবি থেকে PDF", "JPG PDF converter"] });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

import { createToolMetadata } from "@/lib/seo";
export const metadata = createToolMetadata({ title: "PDF Compressor", description: "PDF ফাইল browser-এ optimize করে download করুন।", path: "/tools/pdf-compressor", keywords: ["PDF compressor", "PDF সাইজ কমান", "compress PDF online"] });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
